
class Angka
{
private:
    /* data */
    bool a,b,c,d,e,f,g,p;
public:
    Angka(/* args */);
    void setNumber(unsigned char angka);
    bool getSegment( unsigned char bit);
    ~Angka();
};
